struct Local {
    backend_id: string, /** format: uuid */
    path: string,
}

export global LocalBackendRepository {
    property <int> name;
    pure callback edit(Local)->bool;
    pure callback get(string /** backend_id */)->{err:bool,result:Local};
}