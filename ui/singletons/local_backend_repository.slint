import {Error} from "../lib/error.slint";
struct Local {
    backend_id: string, /** format: uuid */
    path: string,
}

export global LocalBackendRepository {
    property <int> name;
    pure callback edit(Local)->Error;
    pure callback get(string /** backend_id */)->{err:Error,result:Local};
}